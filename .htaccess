RewriteEngine On

# Only redirect to index.php if the requested file or directory doesn't actually exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite everything to index.php and pass the path as the "url" parameter
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
